name: Publish to NuGet

on:
  workflow_dispatch:
    inputs:
      version:
        description: 'Package version (e.g., 0.5.0)'
        required: true
        type: string
      dry-run:
        description: 'Dry run — pack only, skip push'
        required: false
        type: boolean
        default: false

env:
  DOTNET_VERSION: '10.0.x'
  DOTNET_NOLOGO: true
  DOTNET_CLI_TELEMETRY_OPTOUT: true
  PROJECT_PATH: mcp/CWM.RoslynNavigator/src/CWM.RoslynNavigator.csproj
  TEST_PATH: mcp/CWM.RoslynNavigator/tests/CWM.RoslynNavigator.Tests.csproj

jobs:
  test:
    name: Test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - name: Setup .NET
        uses: actions/setup-dotnet@v5
        with:
          dotnet-version: ${{ env.DOTNET_VERSION }}

      - name: Test
        run: dotnet test ${{ env.TEST_PATH }} -c Release --logger trx --results-directory TestResults

      - name: Upload test results
        uses: actions/upload-artifact@v7
        if: always()
        with:
          name: test-results
          path: TestResults/*.trx

  pack:
    name: Pack
    runs-on: ubuntu-latest
    needs: test
    steps:
      - uses: actions/checkout@v5
        with:
          fetch-depth: 0

      - name: Setup .NET
        uses: actions/setup-dotnet@v5
        with:
          dotnet-version: ${{ env.DOTNET_VERSION }}

      - name: Pack
        run: dotnet pack ${{ env.PROJECT_PATH }} -c Release -o ./nupkg /p:Version=${{ inputs.version }} /p:ContinuousIntegrationBuild=true

      - name: Upload package
        uses: actions/upload-artifact@v7
        with:
          name: nuget-package
          path: ./nupkg/*

  publish:
    name: Publish
    runs-on: ubuntu-latest
    needs: pack
    if: ${{ inputs.dry-run == false }}
    environment: nuget
    steps:
      # Checkout first — it cleans the workspace, so the artifact download must come after.
      - uses: actions/checkout@v5

      - name: Download package
        uses: actions/download-artifact@v8
        with:
          name: nuget-package
          path: ./nupkg

      - name: Count MCP tools
        id: toolcount
        # Compute the tool count from source so the release body never drifts
        # from the actual number of [McpServerTool] attributes.
        run: |
          count=$(find mcp -type f -path '*/Tools/*.cs' -print0 | xargs -0 grep -hoE '\[McpServerTool\(' | wc -l | tr -d ' ')
          echo "Found $count MCP tool(s)"
          if [ "$count" -gt 0 ]; then
            echo "summary=$count read-only Roslyn MCP tools for AI-powered .NET codebase navigation." >> "$GITHUB_OUTPUT"
          else
            echo "summary=Read-only Roslyn MCP tools for AI-powered .NET codebase navigation." >> "$GITHUB_OUTPUT"
          fi

      - name: Setup .NET
        uses: actions/setup-dotnet@v5
        with:
          dotnet-version: ${{ env.DOTNET_VERSION }}

      - name: Push to NuGet.org
        run: dotnet nuget push ./nupkg/CWM.RoslynNavigator.${{ inputs.version }}.nupkg --api-key ${{ secrets.NUGET_API_KEY }} --source https://api.nuget.org/v3/index.json --skip-duplicate

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v3
        with:
          tag_name: v${{ inputs.version }}
          name: CWM.RoslynNavigator v${{ inputs.version }}
          body: |
            ## Install

            ```bash
            dotnet tool install -g CWM.RoslynNavigator --version ${{ inputs.version }}
            ```

            ## What's included

            ${{ steps.toolcount.outputs.summary }}

            See [README](https://github.com/codewithmukesh/dotnet-claude-kit/blob/main/mcp/CWM.RoslynNavigator/README.md) for details.
          generate_release_notes: true
          files: ./nupkg/*
